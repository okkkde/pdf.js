name: Disabled publish website
on:
  workflow_dispatch:

# GitHub Pages deployment disabled in this fork; keep a no-op job to satisfy schema.
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Pages deploy is disabled in this fork"
